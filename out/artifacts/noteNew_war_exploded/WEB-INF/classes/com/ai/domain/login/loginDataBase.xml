<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai.domain.login.LoginData">
    <select id="save" parameterType="LoginData">
        INSERT INTO t_login(login_user, login_ip, login_address,login_time)
        VALUES (#{loginUser},#{loginIp},#{LoginAddress},#{loginTime})
    </select>
</mapper>
